export default class Anuncio {
  constructor(id, titulo, tipoTransaccion, descripcion, precio) {
    this.id = id;
    this.titulo = titulo;
    this.tipoTransaccion = tipoTransaccion;
    this.descripcion = descripcion;
    this.precio = precio;
  }
}
